{
    "scripts": {
        "build": "tsc && copyfiles -u 1 \"src/appsscript.json\" dist",
        "build and push": "tsc && copyfiles -u 1 \"src/appsscript.json\" dist && clasp push"
    },
    "devDependencies": {
        "@types/google-apps-script": "^2.0.7",
        "copyfiles": "^2.4.1"
    }
}
